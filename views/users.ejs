<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users page</title>
    <style>
        th,td,table{
            border: 1px solid black;
            text-align: center;
        }
        body{
            background-color: gray;
            
        }
        button{
            background-color: gray
        }
        button:hover{
            background-color: bisque;
        }
        a{
            text-decoration: none;
            color: black;
        }
    </style>
</head>
<body>
    <h2>All users :</h2><br>
    <a href="/user/new">Create New User</a>
    <table>
        <tr>
            <th>id</th>
            <th>name</th>
        </tr>
        <% for(user of users){ %>
          <tr>
           <td> <%= user.id %></td>
           <td> <%= user.username %></td>
           <td><form method="get" action="/user/<%= user.id %>/edit"><button>Edit Username</button></form></td>
           <td><form method="get" action="/user/<%= user.id %>/delete"><button>Delete User</button></form></td>

          </tr>
        <% } %>
    </table>
</body>
</html>